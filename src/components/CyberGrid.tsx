'use client'

export default function CyberGrid() {
  // The grid and scanlines are handled globally via body::before and body::after in globals.css
  // This component renders any additional ambient elements if needed
  return null
}
